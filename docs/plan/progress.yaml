# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 1
  phase: intelligent-lense
  phase_complete: false
  review_complete: false
  retro_complete: false
  status: running
  last_result: null
  next_task: "[US-L6] Investigate: determine API server approach — Express vs Vite middleware, shared schema module location, system prompt design, dev server setup for dual Vite+API, test strategy for mocking invokeClaude"
  consecutive_fails: 0
  completed_stories: []
