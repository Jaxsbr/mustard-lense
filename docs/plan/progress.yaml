# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 2
  phase: rag-lense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-L10] Investigate: RAG pipeline approach — confirm transformers.js + LanceDB compatibility with TypeScript/Vitest, plan module structure for embedder/indexer/retriever, identify fixture YAML test data shape"
  consecutive_fails: 0
  completed_stories: []
