# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 12
  phase: rag-lense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-L13] Investigate: frontend SSE consumption — review current App.tsx fetch logic, plan SSE stream reading with stage indicators, identify E2E test changes needed"
  consecutive_fails: 0
  completed_stories: [US-L10, US-L11, US-L12]
