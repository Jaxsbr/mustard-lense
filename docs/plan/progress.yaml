# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 6
  phase: rag-lense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-L10] Implement unit tests for indexer and retriever using fixture YAML data — create src/server/rag/rag.test.ts with mocked embedder"
  consecutive_fails: 0
  completed_stories: []
