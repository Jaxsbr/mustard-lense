# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 3
  phase: always-on
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-D6] Add always-on deployment section to ARCHITECTURE.md — setup (copy plist, customize, launchctl load), teardown (launchctl unload), LaunchAgent vs LaunchDaemon rationale"
  consecutive_fails: 0
  completed_stories: [US-D6]
