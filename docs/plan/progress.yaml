# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 2
  phase: always-on
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-D6] Create deploy/com.mustard.lense.plist — LaunchAgent plist template with RunAtLoad, KeepAlive, login shell for PATH, PORT=7777, log paths, template WorkingDirectory"
  consecutive_fails: 0
  completed_stories: []
