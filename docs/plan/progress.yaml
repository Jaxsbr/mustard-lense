# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 8
  phase: structured-browse
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-U3] Investigate: type tabs approach — plan CrudPanel extension with tabs, fetch integration with GET /api/records, count badge, loading state, unit test approach"
  consecutive_fails: 0
  completed_stories: [US-U1, US-U2]
