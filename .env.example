# Path to the mustard data directory containing YAML record files.
# Used by both the browse API (GET /api/records) and the RAG indexer.
# Default: ~/dev/mustard/data
MUSTARD_DATA_DIR=~/dev/mustard/data
