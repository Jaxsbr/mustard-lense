# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 5
  phase: markdown-rich-text
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-R4] Add tests: styled mode typing produces state change, rapid open/close drawer doesn't throw, bold toolbar action produces ** in serialized output"
  consecutive_fails: 0
  completed_stories: [US-R3]
