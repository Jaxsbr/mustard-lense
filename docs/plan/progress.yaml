# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 4
  phase: typography-layout
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-R2] Widen detail drawer to 520px with responsive max-width, restructure drawer body so textarea fills remaining vertical space via flex layout"
  consecutive_fails: 0
  completed_stories: [US-R1]
