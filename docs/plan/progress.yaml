# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 7
  phase: foundation
  phase_complete: false
  review_complete: false
  retro_complete: false
  status: running
  last_result: pass
  next_task: "[US-L4] Investigate: plan smoke test scripts — src/smoke/ directory, smoke:basic and smoke:admin npm scripts, real CLI invocation with permission mode prompt, stdout piping, vitest exclusion"
  consecutive_fails: 0
  completed_stories: [US-L1, US-L2, US-L3]
