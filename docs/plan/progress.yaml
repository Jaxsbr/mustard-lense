# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 3
  phase: intelligent-lense
  phase_complete: false
  review_complete: false
  retro_complete: false
  status: running
  last_result: pass
  next_task: "[US-L6] Implement shared response schema module (src/shared/schema.ts) with TypeScript interfaces for all 5 component types plus LenseResponse wrapper"
  consecutive_fails: 0
  completed_stories: []
