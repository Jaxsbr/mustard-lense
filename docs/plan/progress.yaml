# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 10
  phase: rag-lense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-L12] Investigate: SSE streaming approach — plan how to rewrite POST /api/lense as SSE stream, add POST /api/reindex, update server entry point for startup indexing, identify test changes needed"
  consecutive_fails: 0
  completed_stories: [US-L10, US-L11]
