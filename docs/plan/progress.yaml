# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 8
  phase: rag-lense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-L11] Implement synthesiser interface + CliSynthesiser — create src/server/synthesiser.ts with Synthesiser interface, CliSynthesiser class, and rewrite prompt.ts to inject records inline"
  consecutive_fails: 0
  completed_stories: [US-L10]
