# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.

config:
  verify: "npm run typecheck && npm run lint && npm test && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []

state:
  task_number: 4
  phase: rag-lense
  phase_complete: false
  review_stage: null
  review_complete: false
  retro_complete: false
  pr_number: null
  status: running
  last_result: pass
  next_task: "[US-L10] Implement indexer module — install yaml package, create src/server/rag/indexer.ts that reads all YAML files from configurable path, embeds text field, writes to LanceDB table with metadata columns (id, log_type, capture_date_local, person, status, due_date_local, category, theme, period, tags)"
  consecutive_fails: 0
  completed_stories: []
